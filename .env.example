## Backend Environment Variables (choose either DATABASE_URL OR discrete DB_* values)
PORT=5000
NODE_ENV=development
JWT_SECRET=replace-with-long-random-string

# Option A: Single connection string (preferred in production)
DATABASE_URL=postgres://user:password@host:5432/hospital_schedule

# Option B: Individual components (ignored if DATABASE_URL is set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hospital_schedule
DB_USER=postgres
DB_PASSWORD=postgres_password

# CORS / Frontend origin (set to your Vercel or custom domain in production)
FRONTEND_URL=http://localhost:5173

# Outbound email (if 2FA or notifications via email enabled)
EMAIL_USER=your_smtp_user
EMAIL_PASS=your_smtp_password

## Frontend (place inside frontend/.env or Vercel Project Settings)
VITE_API_URL=http://localhost:5000
