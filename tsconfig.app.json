{
  // This root tsconfig.app.json previously referenced the frontend tsconfig for project references.
  // Removed to avoid composite/noEmit project reference errors. Frontend build uses ./frontend/tsconfig.app.json directly.
}
 